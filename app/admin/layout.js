import AdminDashBoard from "@/components/helper/AdminDashBoard";
import React from "react";

const layout = ({ children }) => {
  return <AdminDashBoard>{children}</AdminDashBoard>;
};

export default layout;
